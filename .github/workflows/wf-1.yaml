name: workflow-1
on: [push]
jobs:
  build-image:
    runs-on: ubuntu-latest
    steps:
      - run: ls -al
      - run: docker build --tag img:latest .
  
  test-application:
    runs-on: ubuntu-latest
    steps:
      - run: python .